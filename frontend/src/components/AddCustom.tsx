import { Box } from "@mui/material";
import { AxiosInstance } from "axios";

export default function AddCustom(props: { requestor: AxiosInstance, isLoggedIn: () => boolean }) {
    return (
        <Box>
            
        </Box>
    )
}